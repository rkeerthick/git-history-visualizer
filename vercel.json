{
  "buildCommand": "ROOT=$(git rev-parse --show-toplevel) && cd $ROOT/shared && npm run build && cd $ROOT/client && npm run build",
  "outputDirectory": "client/dist",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
